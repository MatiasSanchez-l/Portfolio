---
import {NavMenu} from "./Navigation/NavMenu";

const currentLang = (Astro.currentLocale || 'en') as 'en' | 'es';
---

<header class="sticky top-0 z-50 w-full border-b border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60 py-4 px-4 md:px-8">
    <NavMenu client:load currentLang={currentLang}/>
</header>